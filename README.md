# TMCLI 
Simple CLI Tool to manage and plan Tasks.

## Quick Start
```bash
./install.sh
alias tmcli=$HOME/programs/tmcli/tmcli
tmcli -h
```
tmcli mantains a state `state.dat`, which by default is stored in
`$HOME/programs/tmcli/` dir
